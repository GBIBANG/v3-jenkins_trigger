pipeline {
    agent any
    
    triggers {
        pollSCM('* * * * *')
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build') {
            steps {
                sh 'mvn clean install -DskipTests -T1C'
            }
        }

        stage('Commit and Push') {
            steps {
                script {
                    sh "git config --global user.email 'fvgarcin2@gmail.com'"
                    sh "git config --global user.name 'GBIBANG'"
                    sh 'git add .'
                    sh "git commit -m 'Successful build après vérification'"
                    sh 'git push origin master'
                }
            }
        }       
    }

    post {
        always {
            cleanWs()
        }
    }
}
